<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>방명록 페이지</title>
    <!-- Axios CDN -->
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script defer src="/static/visitor.js"></script>
  </head>
  <body>
    <h2>방명록 페이지</h2>
    <form name="visitor-form">
      <fieldset>
        <legend>방명록 등록</legend>
        <input type="text" name="name" placeholder="사용자 이름" /><br />
        <input
          type="text"
          name="comment"
          placeholder="간단 방명록 작성"
        /><br />
        <div id="btn-group">
          <button type="button" onclick="createVisitor()">방명록 등록</button>
          <!-- button 타입으로 아무 능력이 없기에 onclick함수를 걸어줌 -->
        </div>
      </fieldset>
    </form>

    <table border="1" cellspacing="0">
      <thead>
        <tr>
          <th>ID</th>
          <th>작성자</th>
          <th>방명록</th>
          <th>수정</th>
          <th>삭제</th>
        </tr>
      </thead>
      <!-- 
      서버에서 주는 데이터
      data:[
       { id: 1, name: '홍길동', comment: '내 가 왔다.' },
       { id: 2, name: '이찬혁', comment: '으 라차차' }
      ]
      -->
      <tbody>
        <!-- <tr>
          <td>1</td>
          <td>홍길동</td>
          <td>내가 왔다</td>
          <td><button>수정</button></td>
          <td><button>삭제</button></td> -------- 1개의 객체에대한 data형식 이 형식에 맞춰서 밑에 전체 data만들어주기
        </tr> -->
        <% for(let i=0; i< data.length ; i++ ){ %>
        <!-- 기본 for문 i는0부터 배열의 길이보다 작을 때에 for문을 돌린다. -->
        <tr id="tr_<%= data[i].id %>">
          <!-- data i 번째의 id, name, comment -->
          <td><%= data[i].id %></td>
          <td><%= data[i].name %></td>
          <td><%= data[i].comment %></td>
          <td>
            <button onclick="editVisitor('<%= data[i].id %>')">수정</button>
          </td>
          <td>
            <button onclick="deleteVisitor(this,'<%= data[i].id %>')">
              삭제
            </button>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </body>
</html>
