<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼전송 결과</title>
    <style>
      span {
        color: coral;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1><%=title%> 폼결과 확인하기</h1>
    <p><span><%=userInfo.id%>(<%=userInfo.email%>)</span>님 환영합니다</p>
    <p>비밀번호는 <span><%=userInfo.pw%></span> 군요!!</p>
    <a href="/">홈으로 이동</a>

    <!-- <h1><%=title%> 폼결과 확인하기</h1> -->
    <!--< % % >를 이용해서 값을 전달해줌 title이라는 변수를 적용 시켜야 하므로 '=' 사용-->
    <!-- <p><span>id: <%=userInfo.id%> / <%=userInfo.email%></span>님 환영합니다</p> -->
    <!--req.query가 객체이기 때문에 userInfo.을 해줘야 한다.-->
    <!-- <p>비밀번호는<span><%=userInfo.pw%></span>이네요</p> -->
    <!-- <a href="/">홈으로 이동</a> -->
  </body>
</html>
